.nh
.TH "CIRCLECI-MIGRATE" "1" "Jun 2026" "circleci-migrate" "CircleCI Org Migration CLI"

.SH NAME
circleci-migrate-secrets - Capture secret values from inside a CircleCI job.


.SH SYNOPSIS
\fBcircleci-migrate secrets [flags]\fP


.SH DESCRIPTION
secrets handles the one thing the API cannot: secret VALUES.

.PP
CircleCI masks environment-variable values everywhere in its API, so export
captures only their names. Inside a running job, however, a context's (or
project's) variables are injected as ordinary environment variables. The
\&'extract' subcommand reads those values from the job environment — using the
names recorded in the export manifest — and writes them to a secret bundle.

.PP
This command is meant to run INSIDE a CircleCI job (see the generated workflow
and the orb). The resulting bundle contains plaintext secrets: protect it,
keep it out of version control, and delete it once the sync is complete.


.SH OPTIONS
\fB-h\fP, \fB--help\fP[=false]
	help for secrets


.SH OPTIONS INHERITED FROM PARENT COMMANDS
\fB--debug\fP[=false]
	Enable debug logging

.PP
\fB--dest-token\fP=""
	API token for the destination org (env: CIRCLECI_DEST_TOKEN)

.PP
\fB--host\fP="https://circleci.com"
	CircleCI host URL (env: CIRCLECI_HOST)

.PP
\fB--source-token\fP=""
	API token for the source org (env: CIRCLECI_SOURCE_TOKEN)

.PP
\fB--token\fP=""
	Personal API token — fallback for both orgs (env: CIRCLECI_CLI_TOKEN)


.SH SEE ALSO
\fBcircleci-migrate(1)\fP, \fBcircleci-migrate-secrets-capture(1)\fP, \fBcircleci-migrate-secrets-decrypt(1)\fP, \fBcircleci-migrate-secrets-extract(1)\fP, \fBcircleci-migrate-secrets-merge(1)\fP


.SH HISTORY
10-Jun-2026 Auto generated by spf13/cobra
