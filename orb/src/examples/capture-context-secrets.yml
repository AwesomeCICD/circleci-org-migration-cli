description: |
  Capture secrets for two contexts (one job each, each referencing its
  context) and merge them into a single secrets.json artifact. Commit your
  exported manifest.json to the repo first.
usage:
  version: "2.1"
  orbs:
    migrate: awesomecicd/circleci-org-migration@volatile
  workflows:
    capture-secrets:
      jobs:
        - migrate/extract_context:
            name: extract-deploy-prod
            context_name: deploy-prod
            context:
              - deploy-prod
        - migrate/extract_context:
            name: extract-shared
            context_name: shared
            context:
              - shared
        - migrate/merge:
            name: merge-secrets
            requires:
              - extract-deploy-prod
              - extract-shared
